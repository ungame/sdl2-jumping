#ifndef COLORS_H
#define COLORS_H

#include <SDL2/SDL.h>

const SDL_Color Red = { 255, 0, 0, SDL_ALPHA_TRANSPARENT };
const SDL_Color Green = { 0, 255, 0, SDL_ALPHA_TRANSPARENT };
const SDL_Color Blue = { 0, 0, 255, SDL_ALPHA_TRANSPARENT };
const SDL_Color Black = { 0, 0, 0, SDL_ALPHA_TRANSPARENT };
const SDL_Color White = { 255, 255, 255, SDL_ALPHA_TRANSPARENT };

#endif